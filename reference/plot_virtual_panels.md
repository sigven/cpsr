# Function that makes a HTML display of virtual gene panel

Function that makes a HTML display of virtual gene panel

## Usage

``` r
plot_virtual_panels(gene_df)
```

## Arguments

- gene_df:

  data frame with genes targeted in virtual panel
