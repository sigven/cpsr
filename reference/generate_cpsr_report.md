# Function that generates variant predisposition report - CPSR

Function that generates variant predisposition report - CPSR

## Usage

``` r
generate_cpsr_report(yaml_fname = NULL)
```

## Arguments

- yaml_fname:

  YAML file with paths to pre-processed molecular input files,
  configuration settings, metadata for reference data bundle etc.
