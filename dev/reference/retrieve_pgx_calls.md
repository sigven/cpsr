# Function that retrieves variants in genes recommended for secondary findings

Function that retrieves variants in genes recommended for secondary
findings

## Usage

``` r
retrieve_pgx_calls(calls)
```

## Arguments

- calls:

  data frame with all calls found
