# Function that combines classifications of novel and pre-classified variants

Function that combines classifications of novel and pre-classified
variants

## Usage

``` r
combine_novel_and_preclassified(var_calls, conf = NULL)
```

## Arguments

- var_calls:

  variants in cancer predisposition genes

- conf:

  CPSR configuration object with run settings
